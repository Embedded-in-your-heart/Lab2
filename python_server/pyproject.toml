[project]
name = "stm32-sensor-dashboard"
version = "0.1.0"
description = "TCP Server + WebSocket Dashboard for STM32 B-L475E-IOT01A sensor visualization"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "websockets>=13.0",
]

[project.scripts]
dashboard = "server:main"
